{
    "$schema": "http://turbo.build/schema.json",
    "globalDotEnv": [".env"],
    "pipeline": {
        "build": {
            "outputs": ["dist/**"]
        },
        "dev": {
            "persistent": true,
            "cache": false,
            "dotEnv": [".env.development.local", ".env.local", ".env.development", ".env"]
        }
    }

    
}